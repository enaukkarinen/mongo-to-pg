{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Program",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/node_modules/ts-node-dev/lib/bin.js",
      "args": ["--respawn", "--transpile-only", "${workspaceFolder}/src/app.ts"],
      "console": "integratedTerminal",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
